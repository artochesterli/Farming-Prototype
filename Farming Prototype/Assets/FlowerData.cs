using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlowerData : MonoBehaviour
{
    public float NormalSpeed = 0;
    public float StickySlowDownSpeed = 0;

    public float PushFieldRadius = 6.5f;
    public float PushFieldAngle = 90;
    public float PushFieldTurnSpeed = 90;
    public float PushFieldCoolDown = 5;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
